#include "main.h"

/**
 * _abs - function computes the absolute value of an integer
 *
 * @n: integer to check
 *
 * Return: int or int multiply by negative one
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
