#include "main.h"

/**
 * print_rev - print string in reverse
 * @s: string
 *
 * Return: string in reverse
 */
void print_rev(char *s)
{
	int b;

	for (b = (_strlen - 1); b <= (_strlen - 1); b--)
	{
		_putchar(s[b]);
	}
	_putchar('\n');
}
