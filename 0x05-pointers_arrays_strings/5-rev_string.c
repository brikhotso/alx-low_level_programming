#include "main.h"

/**
 * rev_string - print string in reverse
 * @s: string
 *
 * Return: string in reverse
 */
void rev_string(char *s)
{
	int i, b;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	b = i - 1;
	for (i = b; i >= 0; i--)
	{
		return (s[i]);
	}
}
